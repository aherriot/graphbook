# graphbook

project using postgres, express, graphql, and react

`yarn install`

# frontend

`yarn run start`

# backend

`yarn run db:migrate`

`yarn run db:seed`

`yarn server:start`
or
`node server/server.js`
